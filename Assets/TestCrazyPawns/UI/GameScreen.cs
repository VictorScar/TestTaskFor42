using System;
using System.Collections;
using System.Collections.Generic;
using ScarFramework.UI;
using UnityEngine;

public class GameScreen : UIScreen
{
    /*private Vector3 _lastPointerPos;
    private float borderTreshhold;
    private void Update()
    {
        var mousePos = Input.mousePosition;
        var screenPoint = rect.InverseTransformPoint(mousePos);

        CheskScreenBoreders(screenPoint);
        
        _lastPointerPos = screenPoint;
    }

    private void CheskScreenBoreders(Vector3 screenPoint)
    {
        Debug.Log("ScreenPoint = " + screenPoint);
        Debug.Log("screen size = " + rect.sizeDelta.x);
        
        if (screenPoint.x - rect.sizeDelta.x <= borderTreshhold)
        {
            
        }
    }*/
}
